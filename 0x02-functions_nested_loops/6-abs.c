#include "main.h"
#include <stdio.h>

/**
 * _abs - to compute the absolute value of an integer.
 *
 * @i: input number as an integer
 *
 * Return: Absolute value
 */

int _abs(int i)
{
if (1 >= 0)
{
return (i);
}
else
{
return (i * -1);
}
}
